# Copy to .env and adjust. Defaults shown are what the code falls back to.

# URL of the game server. The client opens its websocket here.
NEXT_PUBLIC_WEBSOCKET_URL=http://localhost:8000

# Client log verbosity: trace, debug, info, warn or error.
NEXT_PUBLIC_LOG_LEVEL=info
