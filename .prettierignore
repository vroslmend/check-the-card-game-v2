# Build and install output.
node_modules/
.next/
out/
dist/

# Generated. npm owns the lockfile and Next owns the env shim, and formatting
# either one only picks a fight with the tool that rewrites it.
package-lock.json
next-env.d.ts
*.tsbuildinfo

# Local. These never reach a clean checkout, so formatting them would make a
# local run disagree with CI, which is the whole thing this file just stopped
# doing.
.env*
*.log
.DS_Store
.claude/
.gemini/
